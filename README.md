# Enjoy-Vim in Docker

Enjoy-vim in Docker.

**build:**

```shell
docker build -t nvim .
```

**run:**

```shell
docker run -itd --name nvim nvim
```

**exec:**

```shell
docker exec -it nvim bash
```
